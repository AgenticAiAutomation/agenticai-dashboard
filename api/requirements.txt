fastapi==0.109.0
uvicorn[standard]==0.27.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt 5.x — keep this pinned.
bcrypt==4.0.1
python-multipart==0.0.6
# 2.9.9 shipped no wheels for Python 3.13+, so pip tried to compile from source
# and failed on any modern interpreter. 2.9.10 is a wheels-only patch release —
# same library, no API change.
psycopg2-binary==2.9.10
sqlalchemy==2.0.25
alembic==1.13.1
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.1
python-dotenv==1.0.0

# --- SEO Operations module ---
httpx==0.26.0
anthropic==0.69.0
markdown==3.5.2
minio==7.2.5
# Same reason as psycopg2: 10.2.0 predates Python 3.13 wheels. We use exactly
# one Pillow call — Image.open(...).size in routes/articles.py — which is
# unchanged across 10.x and 11.x, so the major bump carries no risk here.
pillow==11.3.0
pyotp==2.9.0
google-auth==2.28.1
google-api-python-client==2.120.0
google-analytics-data==0.18.5
