# Block schema

Generated by `python -m scripts.gen_block_schema_doc` from `app/blog_engine/schema.py` and the Phase 1 fixture. Do not edit by hand — change the schema and regenerate.

## Envelope

```json
{
  "id": "blk_7f3a",
  "type": "image",
  "version": 1,
  "attrs": {},
  "content": []
}
```

- `id` — stable across edits, `blk_` + 4–24 alphanumerics, unique within the article. Used for anchors, comments and revision diffs.
- `type` — one of the 21 types below.
- `version` — schema version of this block, currently `1`.
- `attrs` — per-type attributes (tables below).
- `content` — per-type body: rich text, items, rows, or nothing.

## Inline text (rich text)

Wherever a block says *rich text*, the value is a list of runs. A bare string or a single run is also accepted and normalised.

```json
[
  {
    "text": "It was just "
  },
  {
    "text": "slow",
    "marks": [
      {
        "type": "highlight",
        "attrs": {
          "token": "amber"
        }
      }
    ]
  },
  {
    "text": "."
  }
]
```

Marks: `bold`, `italic`, `underline`, `strike`, `code`, `link`, `highlight`, `emphasis`, `sup`, `sub`.

- `link` takes `attrs.href` (http(s), site-relative, `#anchor` or `mailto:`), optional `rel` (`nofollow`, `sponsored`, `ugc`), `target` (`_blank` only), `title`. External links get `rel="noopener"` automatically.
- `highlight` takes `attrs.token`, one of `amber`, `mint`, `sky`, `rose` — a colour name, never a hex value.
- `emphasis` takes `attrs.token`, one of `brand`, `accent`, `positive`, `warning`.
- Every other mark takes no attrs.

## Block types

| # | type | purpose | SEO output | counts as a visual |
|---|---|---|---|---|
| 1 | `paragraph` | Body text. | — | — |
| 2 | `heading` | h2/h3/h4 only; h1 is the article title. | auto `id` slug, feeds the TOC | — |
| 3 | `key_takeaways` | 3–5 bullets, pinned near the top. | featured snippet / AI overview target | — |
| 4 | `list` | bullet, numbered or checklist. | — | — |
| 5 | `steps` | Ordered how-to, optional image per step. | `HowTo` when ≥3 steps | yes |
| 6 | `image` | Figure with caption and credit. | `ImageObject`; alt required | yes |
| 7 | `gallery` | 2–4 images in a grid. | `ImageObject[]` | yes |
| 8 | `compare_slider` | Before/after; two images side by side without JS. | `ImageObject[]` | yes |
| 9 | `video` | Self-hosted clip or YouTube facade (no iframe until click). | `VideoObject` | yes |
| 10 | `table` | Header row, alignment, caption; scroll or stack on mobile. | table snippet eligible | yes |
| 11 | `callout` | info / tip / warning / result. | — | yes |
| 12 | `quote` | Pull quote with attribution, role, company. | `Quotation` | yes |
| 13 | `code` | Language label; copy button appears with JS. | — | yes |
| 14 | `stat_band` | 2–4 big numbers; count-up with JS. | — | yes |
| 15 | `chart` | bar / line / donut from a data table, as inline SVG. | hidden data table in the DOM | yes |
| 16 | `process_flow` | Left→right flow with a CSS-animated pulse, as inline SVG. | hidden ordered list in the DOM | yes |
| 17 | `faq` | Q/A pairs as a `<details>` accordion. | `FAQPage` | — |
| 18 | `cta` | Link to a service page or Calendly. | — | — |
| 19 | `divider` | Section break: line / dots / glyph. | — | — |
| 20 | `embed` | LinkedIn / X facade — a labelled link, no third-party script. | — | — |
| 21 | `legacy_html` | Migration only. Allowlist-sanitised passthrough. | — | — |

### `paragraph`

Body text.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `variant` | `normal` \| `lead` | `"normal"` |  |

**content** — list of [Run](#run).

```json
{
  "id": "blk_p001",
  "type": "paragraph",
  "attrs": {
    "variant": "lead"
  },
  "content": [
    {
      "text": "Every automation vendor has a slide with a 90% number on it. This is the story of what the number looked like on a real accounts-payable desk in Bhiwandi, with the parts that went wrong left in."
    }
  ]
}
```

### `heading`

h2/h3/h4 only; h1 is the article title.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `level` | `2` \| `3` \| `4` | required |  |
| `gradient` | bool | `false` |  |
| `id` | str (optional) | — |  |

**content** — list of [Run](#run).

```json
{
  "id": "blk_h001",
  "type": "heading",
  "attrs": {
    "level": 2,
    "gradient": true
  },
  "content": [
    {
      "text": "What actually breaks in production"
    }
  ]
}
```

### `key_takeaways`

3–5 bullets, pinned near the top.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `title` | str | `"Key takeaways"` |  |

**content** — list of list of [Run](#run).

```json
{
  "id": "blk_kt01",
  "type": "key_takeaways",
  "content": [
    [
      {
        "text": "Manual keying of 40,000 invoices a year cost 2.3 FTE; the automated pipeline runs on 0.4 FTE of exception handling."
      }
    ],
    [
      {
        "text": "Document AI read 91% of invoices cleanly on day one; the remaining 9% were three vendor layouts, fixed with templates, not models."
      }
    ],
    [
      {
        "text": "Cycle time fell from 6.1 days to 1.6 days; the 74% figure is measured at the ninety-day mark, not the demo."
      }
    ],
    [
      {
        "text": "Total spend was ₹18.4 lakh including licences; payback landed in month seven."
      }
    ]
  ]
}
```

### `list`

bullet, numbered or checklist.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `style` | `bullet` \| `numbered` \| `checklist` | `"bullet"` |  |

**content** — list of [ListItem](#listitem).

```json
{
  "id": "blk_l001",
  "type": "list",
  "attrs": {
    "style": "numbered"
  },
  "content": [
    [
      {
        "text": "Week 1–2: shadow mode. The pipeline ran beside the manual process; nothing it produced was posted."
      }
    ],
    [
      {
        "text": "Week 3–6: assisted mode. Humans approved every bot posting from the queue."
      }
    ],
    [
      {
        "text": "Week 7 onward: exception mode. Only low-confidence items reach a person."
      }
    ]
  ]
}
```

### `steps`

Ordered how-to, optional image per step.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `title` | str (optional) | — |  |
| `total_time` | str (optional) | — |  |

**content** — list of [Step](#step).

```json
{
  "id": "blk_st01",
  "type": "steps",
  "attrs": {
    "title": "How the confidence gate is set up",
    "total_time": "PT45M"
  },
  "content": [
    {
      "title": "Log every extraction with its confidence score",
      "body": [
        {
          "text": "Document AI returns a per-field confidence. Store all of them, not just the failures — you need the distribution to pick a threshold."
        }
      ]
    },
    {
      "title": "Pick the threshold from the data, not from a default",
      "body": [
        {
          "text": "We plotted a week of scores. 0.85 caught 96% of the wrong reads while sending only 11% of invoices to a human. The vendor default of 0.7 would have let through 3× as many errors."
        }
      ]
    },
    {
      "title": "Route low-confidence items to a queue with the image beside the fields",
      "body": [
        {
          "text": "The reviewer sees the scan and the extracted values side by side and corrects only the flagged field. Average handling time: 40 seconds."
        }
      ]
    }
  ]
}
```

### `image`

Figure with caption and credit.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `asset_id` | str (optional) | — |  |
| `src` | str | required |  |
| `alt` | str | required |  |
| `caption` | str (optional) | — |  |
| `credit` | str (optional) | — |  |
| `width` | int | required |  |
| `height` | int | required |  |
| `layout` | `inset` \| `full` \| `breakout` | `"inset"` |  |
| `priority` | bool | `false` |  |
| `link` | str (optional) | — |  |
| `variants` | list of [ImageVariant](#imagevariant) | `[]` |  |
| `lqip` | str (optional) | — |  |

**content** — list of Any.

```json
{
  "id": "blk_i001",
  "type": "image",
  "attrs": {
    "src": "/static/blog/engine-demo/failures-board.jpg",
    "alt": "Whiteboard listing the five invoice failure causes with tally marks, carbon-copy forms far ahead",
    "caption": "The failure board on day three. Carbon-copy forms from one carrier caused more rejects than the other four causes combined.",
    "credit": "Agentic AI Automation",
    "width": 1600,
    "height": 900,
    "layout": "breakout"
  }
}
```

### `gallery`

2–4 images in a grid.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `layout` | `inset` \| `breakout` | `"breakout"` |  |
| `caption` | str (optional) | — |  |

**content** — list of [ImageAttrs](#imageattrs).

```json
{
  "id": "blk_g001",
  "type": "gallery",
  "attrs": {
    "caption": "The two layouts that caused most of the rejects, before and after the scanner preset change."
  },
  "content": [
    {
      "src": "/static/blog/engine-demo/scan-bad.jpg",
      "alt": "Low-contrast carbon-copy invoice scan with faint totals",
      "width": 1200,
      "height": 900
    },
    {
      "src": "/static/blog/engine-demo/scan-good.jpg",
      "alt": "Same invoice rescanned with the high-contrast preset, totals legible",
      "width": 1200,
      "height": 900
    }
  ]
}
```

### `compare_slider`

Before/after; two images side by side without JS.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `before` | [ImageAttrs](#imageattrs) | required |  |
| `after` | [ImageAttrs](#imageattrs) | required |  |
| `label_before` | str | `"Before"` |  |
| `label_after` | str | `"After"` |  |
| `layout` | `inset` \| `breakout` | `"breakout"` |  |
| `caption` | str (optional) | — |  |

**content** — list of Any.

```json
{
  "id": "blk_cs01",
  "type": "compare_slider",
  "attrs": {
    "before": {
      "src": "/static/blog/engine-demo/queue-before.jpg",
      "alt": "Spreadsheet-based invoice tracker with 312 rows marked pending in red",
      "width": 1600,
      "height": 900
    },
    "after": {
      "src": "/static/blog/engine-demo/queue-after.jpg",
      "alt": "OpenBots orchestrator dashboard with 27 items in the exception queue and the rest cleared",
      "width": 1600,
      "height": 900
    },
    "label_before": "Spreadsheet, June",
    "label_after": "Orchestrator, September",
    "caption": "Same Monday morning, three months apart."
  }
}
```

### `video`

Self-hosted clip or YouTube facade (no iframe until click).

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `kind` | `file` \| `youtube` | required |  |
| `src` | str (optional) | — |  |
| `youtube_id` | str (optional) | — |  |
| `poster` | str | required |  |
| `title` | str | required |  |
| `description` | str | required |  |
| `upload_date` | str | required |  |
| `duration` | str | required |  |
| `width` | int | `1280` |  |
| `height` | int | `720` |  |
| `loop` | bool | `false` |  |
| `layout` | `inset` \| `breakout` | `"inset"` |  |
| `caption` | str (optional) | — |  |

**content** — list of Any.

```json
{
  "id": "blk_v001",
  "type": "video",
  "attrs": {
    "kind": "youtube",
    "youtube_id": "dQw4w9WgXcQ",
    "poster": "/static/blog/engine-demo/clip-poster.jpg",
    "title": "Ninety seconds inside the exception queue",
    "description": "A reviewer clears three low-confidence invoices in the OpenBots queue, showing the scan beside the extracted fields.",
    "upload_date": "2026-09-10",
    "duration": "PT1M32S",
    "caption": "The exception queue, as the reviewer sees it."
  }
}
```

### `table`

Header row, alignment, caption; scroll or stack on mobile.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `caption` | str | required |  |
| `has_header` | bool | `true` |  |
| `align` | list of `left` \| `right` \| `center` | `[]` |  |
| `sortable` | bool | `false` |  |
| `mobile` | `scroll` \| `stack` | `"scroll"` |  |
| `layout` | `inset` \| `breakout` | `"inset"` |  |

**content** — list of list of list of [Run](#run).

```json
{
  "id": "blk_t001",
  "type": "table",
  "attrs": {
    "caption": "OpenBots vs UiPath vs Power Automate — licence cost per unattended bot per year",
    "has_header": true,
    "align": [
      "left",
      "right",
      "right",
      "left"
    ],
    "sortable": true,
    "mobile": "stack"
  },
  "content": [
    [
      {
        "text": "Platform"
      },
      {
        "text": "Year 1"
      },
      {
        "text": "Year 3"
      },
      {
        "text": "Notes"
      }
    ],
    [
      {
        "text": "OpenBots"
      },
      {
        "text": "₹0"
      },
      {
        "text": "₹0"
      },
      {
        "text": "Open source; support contract optional"
      }
    ],
    [
      {
        "text": "UiPath"
      },
      {
        "text": "₹4,20,000"
      },
      {
        "text": "₹12,60,000"
      },
      {
        "text": "Per unattended robot"
      }
    ],
    [
      {
        "text": "Power Automate"
      },
      {
        "text": "₹1,26,000"
      },
      {
        "text": "₹3,78,000"
      },
      {
        "text": "Per bot, hosted RPA add-on"
      }
    ]
  ]
}
```

### `callout`

info / tip / warning / result.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `kind` | `info` \| `tip` \| `warning` \| `result` | `"info"` |  |
| `title` | str (optional) | — |  |

**content** — list of [Run](#run).

```json
{
  "id": "blk_ca01",
  "type": "callout",
  "attrs": {
    "kind": "tip"
  },
  "content": [
    {
      "text": "Licence cost is the number vendors want you to compare. Exception-handling hours are the number that actually moves. In this project, exceptions went up — from 14 to 21 hours per thousand — because the pipeline now surfaces problems that used to be silently keyed wrong."
    }
  ]
}
```

### `quote`

Pull quote with attribution, role, company.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `attribution` | str | required |  |
| `role` | str (optional) | — |  |
| `company` | str (optional) | — |  |

**content** — list of [Run](#run).

```json
{
  "id": "blk_q001",
  "type": "quote",
  "attrs": {
    "attribution": "Head of Accounts",
    "role": "Finance lead",
    "company": "Client (name withheld)"
  },
  "content": [
    {
      "text": "The thing I did not expect was trusting it. The queue shows me exactly what it was unsure about, so I stopped checking the ones it was sure about after the second week."
    }
  ]
}
```

### `code`

Language label; copy button appears with JS.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `language` | str | `"text"` |  |
| `caption` | str (optional) | — |  |

**content** — str.

```json
{
  "id": "blk_co01",
  "type": "code",
  "attrs": {
    "language": "python",
    "caption": "The confidence gate, in full"
  },
  "content": "THRESHOLD = 0.85\n\ndef route(extraction):\n    low = [f for f, c in extraction.confidence.items() if c < THRESHOLD]\n    if low:\n        return queue.human_review(extraction, fields=low)\n    return openbots.submit(extraction)"
}
```

### `stat_band`

2–4 big numbers; count-up with JS.

**attrs** — none.

**content** — list of [Stat](#stat).

```json
{
  "id": "blk_s001",
  "type": "stat_band",
  "content": [
    {
      "value": "6.1",
      "label": "days per invoice, before",
      "numeric": 6.1,
      "suffix": "d"
    },
    {
      "value": "1.6",
      "label": "days per invoice, after ninety days",
      "numeric": 1.6,
      "suffix": "d"
    },
    {
      "value": "91",
      "label": "read cleanly on day one",
      "numeric": 91,
      "suffix": "%"
    },
    {
      "value": "7",
      "label": "months to payback",
      "numeric": 7
    }
  ]
}
```

### `chart`

bar / line / donut from a data table, as inline SVG.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `kind` | `bar` \| `line` \| `donut` | required |  |
| `title` | str | required |  |
| `unit` | str | `""` |  |
| `labels` | list of str | required |  |
| `series` | list of [ChartSeries](#chartseries) | required |  |
| `layout` | `inset` \| `breakout` | `"inset"` |  |
| `source` | str (optional) | — |  |

**content** — list of Any.

```json
{
  "id": "blk_c001",
  "type": "chart",
  "attrs": {
    "kind": "bar",
    "title": "Hours per 1,000 invoices, by stage",
    "unit": "h",
    "labels": [
      "Keying",
      "PO match",
      "Approval chase",
      "Exceptions"
    ],
    "series": [
      {
        "name": "Before",
        "values": [
          112,
          46,
          38,
          14
        ]
      },
      {
        "name": "After",
        "values": [
          0,
          4,
          6,
          21
        ]
      }
    ],
    "layout": "breakout",
    "source": "Client time-and-motion study, June and September 2026"
  }
}
```

### `process_flow`

Left→right flow with a CSS-animated pulse, as inline SVG.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `title` | str (optional) | — |  |
| `layout` | `inset` \| `breakout` | `"breakout"` |  |

**content** — list of [FlowNode](#flownode).

```json
{
  "id": "blk_f001",
  "type": "process_flow",
  "attrs": {
    "title": "Invoice pipeline after the build",
    "layout": "breakout"
  },
  "content": [
    {
      "label": "Vendor email",
      "sub": "PDF / scan",
      "kind": "system"
    },
    {
      "label": "Document AI",
      "sub": "extract fields",
      "kind": "bot"
    },
    {
      "label": "Confidence gate",
      "sub": "< 0.85 → human",
      "kind": "decision"
    },
    {
      "label": "OpenBots",
      "sub": "PO match + Tally",
      "kind": "bot"
    },
    {
      "label": "Approver",
      "sub": "WhatsApp tap",
      "kind": "human"
    },
    {
      "label": "Paid",
      "sub": "1.6 days avg",
      "kind": "result"
    }
  ]
}
```

### `faq`

Q/A pairs as a `<details>` accordion.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `title` | str | `"Frequently asked questions"` |  |

**content** — list of [FaqItem](#faqitem).

```json
{
  "id": "blk_fq01",
  "type": "faq",
  "content": [
    {
      "question": "Does this need UiPath, or does OpenBots do the whole job?",
      "answer": [
        {
          "text": "OpenBots ran the entire posting and matching flow here. UiPath was evaluated and would have added roughly ₹4.2 lakh per bot per year in licences for the same work. The document-AI step is a separate service either platform can call."
        }
      ]
    },
    {
      "question": "How long did the build take?",
      "answer": [
        {
          "text": "Eleven weeks from kickoff to exception mode, including two weeks of shadow running. The build itself was about six weeks; the rest was the confidence threshold, vendor scanner fixes and approver training."
        }
      ]
    },
    {
      "question": "What happens when a vendor changes their invoice layout?",
      "answer": [
        {
          "text": "The confidence gate catches it. New layouts score low, go to a human, and the corrections feed a template for that vendor. In ninety days this happened four times and never reached the ledger uncorrected."
        }
      ]
    },
    {
      "question": "Is 74% typical?",
      "answer": [
        {
          "text": "No single number is typical. This client had a long approval chase and a spreadsheet matching step, both of which vanished. A firm that already had an ERP with PO matching would see less on cycle time and more on keying hours."
        }
      ]
    },
    {
      "question": "Who owns the code afterwards?",
      "answer": [
        {
          "text": "The client does, in their own repository, with the runbook. That is standard on every build we do — one process, one price, one date, and the code is yours."
        }
      ]
    }
  ]
}
```

### `cta`

Link to a service page or Calendly.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `heading` | str | required |  |
| `text` | str (optional) | — |  |
| `button_label` | str | required |  |
| `href` | str | required |  |
| `kind` | `service` \| `calendly` \| `contact` | `"service"` |  |

**content** — list of Any.

```json
{
  "id": "blk_cta1",
  "type": "cta",
  "attrs": {
    "heading": "Have an accounts-payable desk like this one?",
    "text": "Bring one process you already know is slow to a 45-minute audit. We will tell you whether document AI, RPA, or a scanner preset is the actual fix.",
    "button_label": "Book the free audit",
    "href": "https://calendly.com/agenticaiautomation",
    "kind": "calendly"
  }
}
```

### `divider`

Section break: line / dots / glyph.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `style` | `line` \| `dots` \| `glyph` | `"line"` |  |

**content** — list of Any.

```json
{
  "id": "blk_d001",
  "type": "divider",
  "attrs": {
    "style": "glyph"
  }
}
```

### `embed`

LinkedIn / X facade — a labelled link, no third-party script.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `provider` | `linkedin` \| `x` | required |  |
| `url` | str | required |  |
| `title` | str (optional) | — |  |
| `preview_text` | str (optional) | — |  |

**content** — list of Any.

```json
{
  "id": "blk_e001",
  "type": "embed",
  "attrs": {
    "provider": "linkedin",
    "url": "https://www.linkedin.com/company/agenticaiautomation/",
    "title": "Agentic AI Automation on LinkedIn",
    "preview_text": "The post announcing the go-live, with the accounts team's own numbers."
  }
}
```

### `legacy_html`

Migration only. Allowlist-sanitised passthrough.

**attrs**

| attr | type | default | notes |
|---|---|---|---|
| `html` | str | required |  |
| `source` | `markdown` \| `html` | `"html"` |  |

**content** — list of Any.

```json
{
  "id": "blk_lg01",
  "type": "legacy_html",
  "attrs": {
    "html": "<p>This paragraph was migrated from the old article as-is. It is allowed <strong>bold</strong> and a <a href=\"/services\">link</a>, and anything else is stripped: <script>alert(1)</script><img src=x onerror=alert(1)></p>",
    "source": "html"
  }
}
```

## Shared attribute models

### ImageAttrs

A figure. `src` is the fallback; `variants` become <picture> sources.

| attr | type | default | notes |
|---|---|---|---|
| `asset_id` | str (optional) | — |  |
| `src` | str | required |  |
| `alt` | str | required |  |
| `caption` | str (optional) | — |  |
| `credit` | str (optional) | — |  |
| `width` | int | required |  |
| `height` | int | required |  |
| `layout` | `inset` \| `full` \| `breakout` | `"inset"` |  |
| `priority` | bool | `false` |  |
| `link` | str (optional) | — |  |
| `variants` | list of [ImageVariant](#imagevariant) | `[]` |  |
| `lqip` | str (optional) | — |  |

### ImageVariant



| attr | type | default | notes |
|---|---|---|---|
| `src` | str | required |  |
| `width` | int | required |  |
| `format` | `avif` \| `webp` \| `jpeg` \| `png` (optional) | — |  |

### Step



| attr | type | default | notes |
|---|---|---|---|
| `title` | list of [Run](#run) | required |  |
| `body` | list of [Run](#run) | `[]` |  |
| `image` | [ImageAttrs](#imageattrs) (optional) | — |  |

### ListItem



| attr | type | default | notes |
|---|---|---|---|
| `content` | list of [Run](#run) | required |  |
| `checked` | bool | `false` |  |

### Stat



| attr | type | default | notes |
|---|---|---|---|
| `value` | str | required |  |
| `label` | str | required |  |
| `numeric` | float (optional) | — |  |
| `prefix` | str | `""` |  |
| `suffix` | str | `""` |  |

### ChartSeries



| attr | type | default | notes |
|---|---|---|---|
| `name` | str | required |  |
| `values` | list of float | required |  |

### FlowNode



| attr | type | default | notes |
|---|---|---|---|
| `label` | str | required |  |
| `sub` | str (optional) | — |  |
| `kind` | `system` \| `bot` \| `human` \| `decision` \| `result` | `"system"` |  |

### FaqItem



| attr | type | default | notes |
|---|---|---|---|
| `question` | str | required |  |
| `answer` | list of [Run](#run) | required |  |
