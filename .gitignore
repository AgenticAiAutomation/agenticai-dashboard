# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
*.egg-info/
dist/
build/

# Environment
.env
.env.local
# web/.env.production is committed on purpose: it holds only the public API URL,
# and the VPS build needs it to bake NEXT_PUBLIC_API_URL into the static export.

# Database
*.db
*.sqlite
*.sql

# Node
node_modules/
.next/
out/
tsconfig.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Alembic
alembic/versions/__pycache__/
error_git.png

# SEO Operations — secrets and generated artefacts that must never be committed
secrets/
CREDENTIALS_JAI.md
SEO_LIVE_APPROVED.txt
uploads/
*service-account*.json

# Backlink Ops — its own env (holds an API key) and its own SQLite file.
# The feature DB lives under api/instance/ (service WorkingDirectory is api/).
# *.db above covers the file itself; this also catches the -wal/-shm sidecars.
ops/backlink-ops.env
api/instance/
backups/
